node_modules/
.DS_Store
*.log
.env
.env.local
# Ignore any local/secret-bearing .npmrc, but keep the committed project one
# (registry/install config only — auth tokens go to $HOME/.npmrc in CI).
.npmrc
!/.npmrc
dist/
build/
.bun/
.next/
next-env.d.ts

# v0.2 eval harness outputs (maintainer-local)
.xera/eval/
# v0.8 coverage runtime outputs (maintainer-local)
.xera/coverage/
.claude/worktrees/
.worktrees/
.claude/launch.json
