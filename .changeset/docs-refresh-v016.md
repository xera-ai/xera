---
---

docs: refresh README, AGENTS, CLAUDE, PROJECT_CONTEXT, ARCHITECTURE, CONFIGURATION for v0.16

Documentation-only refresh of the top-level project docs against the actual state at v0.16.1 — corrected subcommand counts (33 → 34), classifier class counts (8 → 9), public CLI surface (`init`, `doctor`, `samples remove`), GitHub Issues tracker support (in-scope since v0.16, was listed as out-of-scope), `xera doctor --strict [ticket]` arity, and roadmap rows for v0.9 (✅ adversarial), v0.10–v0.15, and v0.16.

No source/code changes — no version bump intended.
